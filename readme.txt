in the multiple shutdown, it reads the different columns as the %% signs, make sure to modify the %% signs when fixing them int the final program.

network commands
net use
ping
ipconfig
netstat -e (interface statistics)
netstat -n -o (active connections)
netstat -a
